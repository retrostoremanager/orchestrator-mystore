name: "Orchestrator"

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  dispatch-tasks:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Process ready tasks
        env:
          GH_TOKEN: ${{ secrets.GH_DISPATCH_TOKEN }}
          ORCHESTRATOR_REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -e
          echo "Scanning for ready tasks..."

          issues=$(gh issue list \
            --repo "$ORCHESTRATOR_REPO" \
            --label ready \
            --state open \
            --json number,title,body,labels \
            --limit 5)

          count=$(echo "$issues" | jq length)
          echo "Found $count ready task(s)"

          if [ "$count" -eq 0 ]; then
            echo "Nothing to do."
            exit 0
          fi

          echo "$issues" | jq -c '.[]' | while read -r issue; do
            number=$(echo "$issue" | jq -r '.number')
            title=$(echo "$issue" | jq -r '.title')
            body=$(echo "$issue" | jq -r '.body // ""')

            target_repo=$(echo "$issue" | jq -r '.labels[].name' | grep '^repo:' | head -1 | sed 's/repo://')

            if [ -z "$target_repo" ]; then
              echo "Issue #$number has no repo: label - skipping"
              continue
            fi

            echo "Dispatching issue #$number to $target_repo"

            prompt="Task from RetroStoreManager Kanban. Issue #${number}: ${title}

${body}

Instructions: Read CLAUDE.md and AGENTS.md first. Create a feature branch feature/issue-${number} off development. Implement the task. Run dotnet build and dotnet test. Open a PR targeting development referencing Closes sbranham314/orchestrator-mystore#${number}"

            gh issue edit "$number" \
              --repo "$ORCHESTRATOR_REPO" \
              --remove-label ready \
              --add-label in-progress

            gh api "repos/sbranham314/${target_repo}/dispatches" \
              --method POST \
              --field event_type=claude-agent \
              --field "client_payload[prompt]=${prompt}" \
              --field "client_payload[branch]=development" \
              --field "client_payload[issue_number]=${number}" \
              --field "client_payload[issue_repo]=${ORCHESTRATOR_REPO}"

            echo "Dispatched issue #$number to $target_repo"
          done
